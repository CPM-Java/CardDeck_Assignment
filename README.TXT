public CardDealer()
Constructor for objects of class CardDeck

public String dealAllCards()
Creates a String representation of the remaining 'undealt' cards from the deck and updates myNumCardsDealt
Returns:
a String representation of the remaining cards

public String dealCards(int cards)
Creates a String representation of the specified number of cards from the deck and updates myNumCardsDealt
Parameters:
cards - the number of cards to be 'dealt' from the 'deck'
Returns:
a String representation of the specified number of cards

public void newDeck()
Creates a new 'deck' of card objects.

public void shuffleDeck()
Performs a 'bunch' of random swaps to 'shuffle' the deck. 
You may use another 'shuffling' implementation
